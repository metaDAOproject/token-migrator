[toolchain]
package_manager = "yarn"

[features]
resolution = true
skip-lint = false

[programs.localnet]
token_migrator = "gr8tqq2ripsM6N46gLWpSDXtdrH6J9jaXoyya1ELC9t"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 10000000 tests/**/*.ts"
initialize = "yarn run ts-node scripts/initialize.ts"
migrate = "yarn run ts-node scripts/migrate.ts"